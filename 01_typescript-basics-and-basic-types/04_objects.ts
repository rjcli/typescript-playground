// Here, we can specify the exact type of an object like the commented region.
// const person: {
//   name: string;
//   age: number;
// } = {
const person = {
  name: 'Maximilian',
  age: 30,
};

console.log(person.name);
